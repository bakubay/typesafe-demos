import { WalkPipelineExplainer } from "@/components/walk-pipeline/walk-pipeline-explainer";

export default function WalkPipelinePage() {
  return (
    <main className="min-h-screen bg-background text-foreground">
      <WalkPipelineExplainer />
    </main>
  );
}
